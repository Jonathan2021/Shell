cmd_ls:
    type: "command_diff"
    command: "ls"

cmd_pwd:
    type: "command_diff"
    command: "pwd"

cmd_help:
    type: "command_diff"
    command: "help"

cmd_echo:
    type: "command_diff"
    command: "echo"

cmd_echo1:
    type: "command_diff"
    command: "echo if if"

cmd_ls_pipe_echo:
    type: "command_diff"
    command: "ls | echo"

cmd_if_true_ls:
    type: "command_diff"
    command: "if true ; then ls; fi"
